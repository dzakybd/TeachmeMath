<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.its.teachmemath">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="com.activeandroid.app.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="AA_MODELS"
            android:value="             id.ac.its.teachmemath.Database.Laporan,             id.ac.its.teachmemath.Database.Materi,             id.ac.its.teachmemath.Database.Soal,             id.ac.its.teachmemath.Database.Tanyajawab,             id.ac.its.teachmemath.Database.User" />
        <meta-data
            android:name="AA_DB_NAME"
            android:value="teachmemath.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />

        <activity android:name=".View.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Login"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".View.Page1"
            android:label="@string/title_activity_page1"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".View.Page2"
            android:label="@string/title_activity_page2"
            android:theme="@style/AppTheme" />
        <activity android:name=".View.Tambahmateri" />
        <activity android:name=".View.Tambahsoal" />
        <activity android:name=".View.Jawabsoal" />
        <activity android:name=".View.Tambahtanyajawab" />
        <activity android:name=".View.Daftar"></activity>
    </application>

</manifest>